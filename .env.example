# Example environment variables for Pomodoro-App-FE
# Copy this file to `.env` and update values for your environment.
# Do NOT commit `.env` if it contains secrets or sensitive data.

# Preferred: split base URL and API version
# Example: API_BASE=https://api.example.com
# API_VERSION=v1
API_BASE=http://localhost:8000
API_VERSION=v1
